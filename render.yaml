services:
  - type: worker
    name: athena-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python botseller.py
    envVars:
      - key: DISCORD_BOT_TOKEN
        sync: false